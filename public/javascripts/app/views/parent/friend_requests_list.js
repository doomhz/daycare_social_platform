// Generated by CoffeeScript 1.3.1
(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor; child.__super__ = parent.prototype; return child; };

  Kin.Parent.FriendRequestsListView = (function(_super) {

    __extends(FriendRequestsListView, _super);

    FriendRequestsListView.name = 'FriendRequestsListView';

    function FriendRequestsListView() {
      this.resendRequestClickHandler = __bind(this.resendRequestClickHandler, this);

      this.cancelRequestClickHandler = __bind(this.cancelRequestClickHandler, this);

      this.render = __bind(this.render, this);
      return FriendRequestsListView.__super__.constructor.apply(this, arguments);
    }

    FriendRequestsListView.prototype.collection = null;

    FriendRequestsListView.prototype.model = null;

    FriendRequestsListView.prototype.tplUrl = '/templates/main/parent/friend_requests_list.html';

    FriendRequestsListView.prototype.events = {
      "click .parent-name": "parentNameClickHandler",
      "submit .friend-request-class-form": "editClassesSubmitHandler",
      "click .cancel-request": "cancelRequestClickHandler",
      "click .resend-request": "resendRequestClickHandler"
    };

    FriendRequestsListView.prototype.initialize = function() {};

    FriendRequestsListView.prototype.render = function() {
      var that;
      that = this;
      return $.tmpload({
        url: this.tplUrl,
        onLoad: function(tpl) {
          var children;
          children = new Kin.ChildrenCollection([], {
            userId: that.model.get("_id")
          });
          return children.fetch({
            success: function() {
              var $el;
              $el = $(that.el);
              $el.html(tpl({
                friendRequests: that.collection,
                profile: that.model,
                classes: that.model.get("daycare_friends"),
                children: children.models
              }));
              return that.$(".chzn-select").chosen();
            }
          });
        }
      });
    };

    FriendRequestsListView.prototype.parentNameClickHandler = function(ev) {
      var $classContainer, $target, requestId;
      ev.preventDefault();
      $target = $(ev.target);
      requestId = $target.data("id");
      $classContainer = this.$("#class-cnt-" + requestId);
      return $classContainer.toggleClass("hidden");
    };

    FriendRequestsListView.prototype.closeEditClassCnt = function(requestId) {
      return this.$("#class-cnt-" + requestId).addClass("hidden");
    };

    FriendRequestsListView.prototype.editClassesSubmitHandler = function(ev) {
      var $target, data, friendRequest, friendRequestId, that;
      ev.preventDefault();
      that = this;
      $target = $(ev.target);
      data = $target.serialize();
      friendRequestId = $target.data("id");
      friendRequest = new Kin.FriendRequestModel({
        _id: friendRequestId
      });
      return friendRequest.save(null, {
        data: data,
        success: function() {
          $.jGrowl("Classes successfully changed");
          return that.closeEditClassCnt(friendRequestId);
        },
        error: function() {
          return $.jGrowl("Classes could not be changed :( Please try again.");
        }
      });
    };

    FriendRequestsListView.prototype.cancelRequestClickHandler = function(ev) {
      var $target, that;
      ev.preventDefault();
      that = this;
      $target = $(ev.target);
      return dConfirm("Are you sure you want to cancel the invite?", function(btType, win) {
        var friendRequest, friendRequestId;
        win.close();
        if (btType === 'yes') {
          friendRequestId = $target.data("id");
          friendRequest = that.collection.get(friendRequestId);
          return friendRequest.destroy({
            wait: true,
            success: function() {
              $.jGrowl("Invite was canceled");
              return that.render();
            },
            error: function() {
              return $.jGrowl("Invite could not be canceled :( Please try again.");
            }
          });
        }
      });
    };

    FriendRequestsListView.prototype.resendRequestClickHandler = function(ev) {
      var $target, that;
      ev.preventDefault();
      that = this;
      $target = $(ev.target);
      return dConfirm("Are you sure you want to resend the invite?", function(btType, win) {
        var friendRequest, friendRequestId;
        win.close();
        if (btType === 'yes') {
          friendRequestId = $target.data("id");
          friendRequest = that.collection.get(friendRequestId);
          return friendRequest.send({
            success: function() {
              return $.jGrowl("Invite was resent");
            },
            error: function() {
              return $.jGrowl("Invite could not be sent :( Please try again.");
            }
          });
        }
      });
    };

    return FriendRequestsListView;

  })(Backbone.View);

}).call(this);
