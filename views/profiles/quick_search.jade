- profiles && profiles.forEach(function (profile) {
    - var profilePicturesSet = _.find(profile.picture_sets, function (pictureSet) {return pictureSet.type === "profile"});
    - if (profilePicturesSet)
      - var avatar = _.find(profilePicturesSet.pictures, function (picture) {return picture.primary === true;});
      - var avatarUrl = avatar ? avatar.mini_url : "";
      - var location = profile.location_components.city && profile.location_components.city ? profile.location_components.city + ", " + profile.location_components.state_code : ""
    #{profile._id}|#{profile.name}|#{profile.surname}|#{avatarUrl || ""}|#{location}
- })