div.login-wrapper
  h1 Change password
  - if ('undefined' !== typeof errors && errors.length)
    div.alert-message.block-message.error
      ul#errors
        - each error in errors
          - if (error === "wrong-user")
            li.error Sorry, but we could not find a profile with the given email.

  - if (success)

    p Please check your email for thurfer instructions about how to change your password. Thank you.

  - else

    form(action='/send-password', method='POST', id='send-pass-form')
      fieldset
        #email.clearfix
          label(for="email") Your email
          .input
            input(type='text', name="email", placeholder="user@example.com")
        #submit.actions
          input.btn.primary(type='submit', value='Change') Change

    p.member Remember your password?
      a(href='/login') Click here to login.